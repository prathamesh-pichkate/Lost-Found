# Server Configuration
PORT=3000

# JWT Secret Key (Set a strong random string in your actual .env file)
JWT_TOKEN_SECRET=<YOUR_JWT_SECRET>

# MongoDB Connection URI (Get it from MongoDB Atlas or local setup)
MONGODB_URL=<YOUR_MONGODB_CONNECTION_STRING>

# Email Configuration (Used for sending OTPs or notifications)
EMAIL_USER=<YOUR_EMAIL_ADDRESS>
EMAIL_PASS=<YOUR_APP_PASSWORD> # Generate from your email provider’s security section

# Client URL (Frontend domain for CORS, redirects, etc.)
CLIENT_URL=<YOUR_FRONTEND_URL>
